# RFI Drafting — Inputs

Drop raw files here before each session:
- Drawing excerpts showing the conflict
- Specification sections referenced
- GC or subcontractor correspondence
- Field condition photos or measurements
- Contract clauses (if relevant)

Files are processed in `/grill-me` → `/rfi` and moved to `_outputs/` when complete.
