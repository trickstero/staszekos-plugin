# Code Research — Inputs

Drop raw files here before each session:
- Project brief (occupancy, height, area, jurisdiction)
- AHJ correspondence
- Prior code analysis
- Drawings (floor plans, sections) for egress review
- Owner/program requirements

Files are processed in `/code-lookup` and moved to `_outputs/` when complete.
